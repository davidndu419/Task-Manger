[build]
  base = "artifacts/api-server"
  command = "pnpm install && pnpm run build"
  publish = "dist"

[functions]
  # This is the new way to write it
  node_bundle = {} 

[[redirects]]
  from = "/*"
  to = "/index"
  status = 200