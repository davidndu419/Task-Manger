[build]
  base = "" # Change this to empty so we start at the root
  command = "pnpm install && pnpm run build" 
  publish = "artifacts/api-server/dist"

[functions]
  directory = "artifacts/api-server/dist"
  node_bundle = {}

[[redirects]]
  from = "/*"
  to = "/index"
  status = 200